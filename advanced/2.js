// 2. Write a function that takes an array of objects and a key, and returns a
// new array sorted based on the values of that key in ascending order.

function sortedArray(arr){
    let sorted = arr.map(el => {

    })
}

let obj = [
    {value: 1},
    {value: 10},
    {value: 30},
    {value: 7},
]

console.log(sortedArray(obj))